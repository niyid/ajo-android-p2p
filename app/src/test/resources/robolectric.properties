# Robolectric Configuration
# Place this file at: app/src/test/resources/robolectric.properties

# Specify the Android SDK version for Robolectric tests
# Use SDK 28 (Android 9.0) for maximum compatibility
sdk=28

# Alternative: if you need to use a higher SDK, try:
# sdk=29  # Android 10
# sdk=30  # Android 11 (requires Robolectric 4.5+)

# Enable incremental compilation (optional, can improve test speed)
# instrumentationEnabled=false
